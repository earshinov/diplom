#pragma once

#include <vector>

void generateResults(int lStart, int lEnd, int mStart, int mEnd);
void generateResults(int lStart, int lEnd, int mStart, int mEnd, const std::vector<bool> & bits, bool one);
